<%= simple_form_for exam, defaults: { input_html: { class: 'default_class' } } do |f| %>
  <%= render 'shared/error_messages', object: f.object  %>

  <%= f.input :name, input_html: { class: 'special'  } %>
  <%= f.button :submit, class: "btn btn-success" %>
<% end %>
