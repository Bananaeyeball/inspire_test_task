<%= form_with(model: exam_question, local: true) do |form| %>
  <% if exam_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(exam_question.errors.count, "error") %> prohibited this exam_question from being saved:</h2>

      <ul>
      <% exam_question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="field">
    <%= form.label :explanation %>
    <%= form.text_area :explanation %>
  </div>

  <div class="field">
    <%= form.label :exam_id %>
    <%= form.number_field :exam_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
